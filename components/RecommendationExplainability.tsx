"use client";

type Item = { source_id: string; source_name: string; recommended_volume_mbd: number; extra_cost_estimate_pct: number; transit_days: number; risk_flag: "low" | "medium" | "high"; priority?: "high" | "medium" | "lower"; rationale: string };
export default function RecommendationExplainability({ recommendations, confidence }: { recommendations: Item[]; confidence: number }) {
  return <section className="rounded-xl border border-slate-200 bg-white p-5 shadow-sm"><div className="flex flex-wrap items-center justify-between gap-2"><div><h2 className="text-xl font-bold">Why this recommendation?</h2><p className="mt-1 text-sm text-slate-600">Factors are illustrative model estimates and live-signal inputs where available.</p></div><span className="rounded-full bg-blue-50 px-3 py-1 text-sm font-bold text-blue-800">Model confidence: {Math.round(confidence * 100)}%</span></div><div className="mt-4 grid gap-3 lg:grid-cols-3">{recommendations.map((item, index) => <article key={item.source_id} className="rounded-lg bg-slate-50 p-4"><p className="text-xs font-bold uppercase tracking-wider text-blue-700">Priority #{index + 1}</p><h3 className="mt-1 font-semibold">{item.source_name}</h3><ul className="mt-3 space-y-1 text-sm text-slate-600"><li>• {item.recommended_volume_mbd.toFixed(2)} mbd illustrative allocation</li><li>• {item.transit_days} days transit vs. baseline</li><li>• {item.extra_cost_estimate_pct > 0 ? "+" : ""}{item.extra_cost_estimate_pct}% cost premium</li><li>• {item.risk_flag} modeled risk · {item.priority ?? "medium"} priority</li><li>• {item.rationale}</li></ul></article>)}</div></section>;
}
